# Database Configuration
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=electronics_db

# JWT Configuration
SECRET_KEY=your-super-secret-key-change-in-production-use-openssl-rand-hex-32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# OTP Configuration
OTP_LENGTH=6
OTP_EXPIRE_MINUTES=10

# Email Configuration (SMTP)
# For Gmail: Use App Password (not regular password)
# 1. Enable 2FA on Google Account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
SMTP_FROM=your-email@gmail.com

# Email Notification Settings (for application status updates)
SMTP_SERVER=smtp.gmail.com
EMAIL_FROM_ADDRESS=hr@electronics.com
EMAIL_FROM_NAME=Electronics HR Team

# SMS Configuration (Twilio)
# Sign up at https://www.twilio.com/
# Get credentials from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_PHONE_NUMBER=+1234567890

# Google OAuth Configuration
# Create OAuth app: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback/google

# Facebook OAuth Configuration
# Create Facebook App: https://developers.facebook.com/apps/
# 1. Create App → Consumer
# 2. Add Facebook Login product
# 3. Add valid OAuth redirect URI: http://localhost:3000/auth/callback/facebook
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret
FACEBOOK_REDIRECT_URI=http://localhost:3000/auth/callback/facebook

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Development Notes:
# - In development, OTP codes will be printed to console (no SMS/Email sent)
# - Configure SMTP and Twilio for production use
# - Keep SECRET_KEY secure and never commit to version control
# - Use environment-specific .env files (.env.dev, .env.prod)
